# Do the lessons run in a browser

Generated by `just build-probe`. Do not edit by hand, the change will be overwritten.

12 lesson(s) on Pyodide 3.14.2: 11 ran end to end, 180 cell(s) in total.

The checks in `report.md` next to this ask whether a surface exists. This runs the lessons themselves: every code cell of every notebook, in order, in one Pyodide runtime, with `pyxray` mounted off the disk rather than installed. The install cell is the one thing changed, and only its `%pip` line, which a reader in a browser does not need either.

| Lesson | Cells | In the browser |
|---|---|---|
| t01-one-line-seven-stages | 18 | runs end to end |
| t02-text-becomes-tokens | 32 | runs end to end |
| t03-tokens-become-a-tree | 13 | runs end to end |
| t04-names-get-scopes | 15 | runs end to end |
| t05-the-tree-becomes-bytecode | 19 | runs end to end |
| t06-reading-bytecode-fluently | 17 | runs end to end |
| t07-the-machine-runs | 15 | 12 of 15 did not run |
| t08-everything-is-an-object | 16 | runs end to end |
| t09-memory-appears-and-disappears | 14 | runs end to end |
| t10-the-napkin | 6 | runs end to end |
| z01-reading-c | 4 | runs end to end |
| z02-being-lost | 11 | runs end to end |

## What did not run

**t07-the-machine-runs, cell t07-14.** Maximum call stack size exceeded

Recursing through `sorted` overflows the WebAssembly call stack, and that is not a Python exception, it kills the runtime. The cell is meant to print a RecursionError and in a browser it takes the tab with it instead. Issue 105.

The 11 cells after it in t07-the-machine-runs never ran. Nothing is retried in a fresh runtime, because a cell that ran in a different interpreter than the cells above it has not been tested, it has been let off.
